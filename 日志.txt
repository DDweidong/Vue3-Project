2024 12 14:
环境搭建：node.js-v20+  //nvm  ; vscode 
创建项目
路由环境安装
创建、配置路由: / , login  
创建相应组件
main.js 项目入口: 引入、注册 路由        ##配置路由名字写错了
ui框架安装  、组件按需导入(看文档)

layout布局：容器、样式的清除(不完整)
aside引入，样式处理  ##body不是全页面        ##复杂->抽离成组件

2024 12 15：
aside部分 实现treemenu：抽离出来做一个组件 -> 二级菜单、侧面隐藏、 动态渲染组件 
-> 由数据来决定渲染的菜单，数据对应着路由组件；
-> 怎么获取数据？：通过路由实例的对象 (未做)
-> 嵌套组件：父传子 ，动态渲染:判断来实现有无二级菜单 (未做)

2024 12 16：
路由相关数据创建好了 

2024 12 17:
拿路由实例数据
实现嵌套treeMenu组件：一级菜单，二级菜单
element plus的图标组件？-> 动态组件的使用

2024 12 18：
菜单导航跳转对应的路由 组件
aside部分样式调整:加在aside，后续的折叠功能


2025  1 7：
hearer静态页面布局：       //HTML结构，css样式 
header关于菜单展开，收起功能：引入vuex ，创建store，创建state（模块的state）；在main中挂载在app中
    菜单的展开，折叠:属性，对应的修改     // vuex ,store 、state ,模块的创建不熟悉
tag的数据添加到state、动态渲染在header上、点击跳转（router-link标签）、选中和没选中的样式（动态class、高亮等）；***//未做***


2025  1  10:
tag的数据添加到state、动态渲染在header上、点击跳转（router-link标签）、选中和没选中的样式（动态class、高亮等）
（tag与菜单的对应高亮 ）//***未完成****
关闭tag：先把state的数据删了，再页面上做跳转的判断


2025  1  11:
登录页面的初步创立：卡片居中（栅格、静态资源引入），样式，表单的创立（数据）（验证码还没弄）
表单的验证码：一个输入框，后缀的发送功能-文字内容、点击事件、手机号验证、倒计时；整个表单的样式
表单的验证，提交键：element plus的使用(还是这个组件的使用)


2025  1 12:
axios的引入与二次封装：两个文件夹-基本的(拦截器，请求前要看看需不需要token头；响应后看看异常处理)、
    api文档-的编写：获取验证码的api（在页面中使用）   //接口，证书问题，ElMessage不行、没有发短信
解决重复倒计时功能，和前缀图标问题
注册和登录的接口联调：表单的整体校验（组件文档的使用），注册的api、点击事件；登录的api、点击事件
用户鉴权：登录页面token的判断-去不了，回不去，token过期处理（权限列表）、（路由守卫）
菜单管理添加功能：新增功能的弹窗效果、内容
弹窗的表单校验（页面），提交逻辑,菜单权限列表


2025  1 13：
（先菜单数据，再账户数据进行关联）
菜单管理列表：组件库使用、编辑按钮(打开弹窗)         //数据流，功能逻辑清楚
    剩余问题：面版(分页效果、样式)，逻辑
账号管理列表：拿数据、表格的渲染（组件的使用）（时间戳的转化）
    分页与编辑弹窗(表单提交记得校验)                //关键还是 数据流 跟 逻辑 


2025  1 14：
退出登录功能的实现：组件库的使用
用户登录，菜单权限数据渲染相应菜单组件： 再store中处理(拿到数据)、动态路由（vite中glob，通过接口返回的泉州把路由拼接成之前写死的形式）//***
动态路由的实现：把拼接好的数据加到router中;动态路由渲染菜单（但是刷新就初始化-需要持久化(菜单、路由);刷新后的动态路由添加）//26逻辑noknow
用户信息（获取数据）的菜单的问题(刷新后的高亮，重定向问题)解决
陪护管理列表：对话框，表单(基本样式)


2025  1  15：
陪护管理列表：添加功能（编辑功能）:上传图片功能（对话框等相关效果、样式、添加逻辑）
    陪护师的列表功能（表格、分页、tag栏）
    

2025  1 16：
陪护管理列表：编辑功能、批量删除功能     //######登录，路由有问题（'/'删了重定向，又可以了）

陪诊系统的C端：项目环境、所需要的包、插件(vant的引用)、路由的创建
tabbar与布局组件(组件的使用)
登录页面显示效果实现(静态)、
逻辑（用户鉴权-token信息）、axios的二次封装、登录接口联调(api在实例上绑定)


2025  1  17:
c端的首页显示：头部、轮播图（拿到首页数据）、首页快捷入口、医院列表  （样式是copy的）
跳转到下单页（点击事件）、下单的页面详情（头部、     //问题：下单页面底部还有tabbar-解决，statusBar也是copy的
    表单、提交按钮）：获取订单数据、选择医院功能     
    选择就诊时间功能
    表单剩余功能完成。


2025  1  18：
订单下单（提交表单）和支付功能：表单校验，(url插件),图片->支付二维码弹窗
订单页面显示：tab栏、请求列表数据


2025  1  19：
订单列表显示：（倒计时copy的）
订单详情页面：获取详情数据，头部，数据渲染
"我的"页面显示     //问题：快捷入口不对应跳，跳了后tabbar栏的高亮（后台管理系统也类似）、点击问题
后台管理系统的订单管理页面   、订单状态扭转       //订单号查询功能，完成
    //******************控制台没完成*********************

2025  1  20：
控制台完成 ， 总体项目大致完成，还有一些小问题
    